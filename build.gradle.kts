// Top-level build file for the MONYMA project.

plugins {
	// No plugins at the root. Plugins are applied per-module via version catalog aliases.
}

allprojects {
	// Place for any shared configuration if needed later.
}